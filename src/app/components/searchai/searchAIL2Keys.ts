/**
 * L2 compound keys for Search AI — must match `searchAIConfig` labels in Sidebar
 * (`Actions` / `Recommendations`, etc.).
 */
export const SEARCH_AI_L2_KEY_RECOMMENDATIONS = "Actions/Recommendations";
export const SEARCH_AI_L2_KEY_TRACK_PROGRESS = "Actions/Track progress";

/** Default L2 selection when opening Search AI (Recommendations 2.0 surface). */
export const SEARCH_AI_L2_DEFAULT_ACTIVE = SEARCH_AI_L2_KEY_RECOMMENDATIONS;
