/** Default L1 rail glyph size (18px − 10%) — Phosphor strip + matching Lucide aux icons. */
export const L1_STRIP_ICON_SIZE = 16.2;
/**
 * Stroke weight on Phosphor roots. For **Lucide**, pass `strokeWidth={L1_STRIP_ICON_STROKE_PX}` and
 * **`absoluteStrokeWidth`** so line weight matches the L1 rail (relative scaling skews thin icons).
 */
export const L1_STRIP_ICON_STROKE_PX = 1.6;
